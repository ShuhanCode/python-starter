t = (1, 2, 3)
l = [1, 2, 3]

print(type(t))
print(type(l))

print(t[0])
print(l[0])

l[0] = 'NEW'

# Immutability
# It can't be stressed enough that tuples are immutable. To drive that point home:

# t[0] = 'NEW'


